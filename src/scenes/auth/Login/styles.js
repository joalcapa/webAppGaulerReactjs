const styles = {
    container: {
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    },
    containerForm: {
        width: 400,
        height: 400,
        alignItems: 'center',
        justifyContent: 'center',
    },
    centerText: {
        textAlign: 'center',
    }
};

export default styles;